# Солон — Failures

Append-only журнал ошибок. Перед каждой задачей — `grep` на ключевые слова,
не наступай на те же грабли. Формат: `YYYY-MM-DD → что предположил → что
оказалось → правило на будущее`. Без реальных данных чужих дел — только
суть ошибки на уровне жанра.
